# Wizard Arsenal configuration — installed to ~/.wizard/config.toml
#
# Generated by the Wizard Arsenal installer. The binary is upstream Wizard;
# this file just ships a fuller default loadout. Docs:
# https://github.com/teddytennant/wizard-arsenal/blob/main/docs/arsenal.md
#
# __MODEL__ is substituted by the installer with the Ollama model tier it
# selected for your hardware (largest single-GPU-fitting Qwen, or the smallest
# tier on CPU-only / undetectable systems).

mode = "genie"
auto_approve = false
max_steps = 25

# Fallback for Wizard builds that predate multi-provider support: these
# top-level keys are read by older binaries that don't understand [[provider]].
model = "__MODEL__"
ollama_host = "http://127.0.0.1:11434"

# Which provider Wizard talks to. Switch live in the TUI with /provider, or
# point this at one of the [[provider]] blocks below by name.
active_provider = "local"

# --- Local Ollama (default) --------------------------------------------------
# Fully local, no API key. The installer pulled this model for you.
[[provider]]
name = "local"
kind = "ollama"
base_url = "http://127.0.0.1:11434"
model = "__MODEL__"

# --- Cloud providers (optional) ----------------------------------------------
# Uncomment a block and set active_provider to its name (or use /provider) to
# route through a hosted model. The API key is read from the named environment
# variable at runtime and is never written to this file.

# [[provider]]
# name = "openai"
# kind = "openai"
# base_url = "https://api.openai.com/v1"
# model = "gpt-4o"
# api_key_env = "OPENAI_API_KEY"

# [[provider]]
# name = "anthropic"
# kind = "anthropic"
# base_url = "https://api.anthropic.com"
# model = "claude-fable-5"
# api_key_env = "ANTHROPIC_API_KEY"
